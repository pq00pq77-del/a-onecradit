const form=document.getElementById('consultForm');const steps=[...document.querySelectorAll('.form-step')];const progressBar=document.getElementById('progressBar');const progressText=document.getElementById('progressText');const toast=document.getElementById('toast');const submitBtn=document.getElementById('submitBtn');let currentStep=0;function showToast(message,error=false){toast.textContent=message;toast.classList.toggle('error',error);toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3200)}function updateStep(){steps.forEach((step,index)=>step.classList.toggle('active',index===currentStep));const percent=[33,66,100][currentStep];progressBar.style.width=percent+'%';progressText.textContent=percent+'%'}function validateStep(){const controls=[...steps[currentStep].querySelectorAll('input,select,textarea')];for(const control of controls){if(!control.checkValidity()){control.reportValidity();return false}}return true}document.querySelectorAll('.next-btn').forEach(button=>button.addEventListener('click',()=>{if(!validateStep())return;currentStep=Math.min(currentStep+1,steps.length-1);updateStep()}));document.querySelectorAll('.prev-btn').forEach(button=>button.addEventListener('click',()=>{currentStep=Math.max(currentStep-1,0);updateStep()}));form.elements.phone.addEventListener('input',event=>{const d=event.target.value.replace(/\D/g,'').slice(0,11);event.target.value=d.length<4?d:d.length<8?d.slice(0,3)+'-'+d.slice(3):d.slice(0,3)+'-'+d.slice(3,7)+'-'+d.slice(7)});form.addEventListener('submit',async event=>{event.preventDefault();if(!validateStep())return;submitBtn.disabled=true;submitBtn.textContent='접수 중...';const payload=Object.fromEntries(new FormData(form).entries());try{const response=await fetch('/api/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.message||'접수 처리 중 오류가 발생했습니다.');showToast('상담 신청이 정상적으로 접수되었습니다.');form.reset();currentStep=0;updateStep()}catch(error){showToast(error.message||'잠시 후 다시 시도해주세요.',true)}finally{submitBtn.disabled=false;submitBtn.textContent='상담 신청 완료'}});
