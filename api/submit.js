export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({message:'허용되지 않은 요청입니다.'});const{name='',phone='',birth='',job='',amount='',contactTime='',message='',privacy=''}=req.body||{};if(!name||!phone||!birth||!job||!amount||!contactTime||!privacy)return res.status(400).json({message:'필수 항목을 모두 입력해주세요.'});const botToken=process.env.TELEGRAM_BOT_TOKEN||process.env.BOT_TOKEN;const chatId=process.env.TELEGRAM_CHAT_ID||process.env.CHAT_ID;if(!botToken||!chatId)return res.status(500).json({message:'텔레그램 환경변수 설정이 필요합니다.'});const e=v=>String(v).replace(/[<>&]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;'}[c]));const text=['📩 <b>A-ONE CREDIT 상담 접수</b>','',`👤 이름: ${e(name)}`,`📱 연락처: ${e(phone)}`,`🎂 생년월일: ${e(birth)}`,`💼 직업: ${e(job)}`,`💰 희망금액: ${e(amount)}`,`🕒 연락 가능 시간: ${e(contactTime)}`,`📝 문의내용: ${e(message||'없음')}`].join('\n');try{const r=await fetch(`https://api.telegram.org/bot${botToken}/sendMessage`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chat_id:chatId,text,parse_mode:'HTML'})});const result=await r.json();if(!r.ok||!result.ok)return res.status(502).json({message:'텔레그램 전송에 실패했습니다.'});return res.status(200).json({ok:true})}catch(error){console.error(error);return res.status(500).json({message:'접수 처리 중 오류가 발생했습니다.'})}}
